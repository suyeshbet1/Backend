# Local development (or non-GCE environments)
PORT=4000

# Option A (recommended on GCE VM): Use Application Default Credentials (ADC)
# - Attach a Service Account to the VM with Firebase/Firestore permissions.
# - Leave the FIREBASE_* variables empty.

# Option B: Explicit Firebase service-account credentials (avoid in production if possible)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=service-account@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
